services:
  quiz:
    build: .
    container_name: intro-quiz
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/data
      - /clips:/clips
